import { AsMetersPerSecondPipe } from './as-meters-per-second.pipe';

describe('AsMetersPerSecondPipe', () => {
  it('create an instance', () => {
    const pipe = new AsMetersPerSecondPipe();
    expect(pipe).toBeTruthy();
  });
});
